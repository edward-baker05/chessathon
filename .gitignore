.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
submission.zip
*.pgn
.pytest_cache/
snapshots/
data/
# The shipped network is added deliberately with `git add -f`, so that a random
# smoke-test net can never be committed and mistaken for a trained one.
weights/*.npz
